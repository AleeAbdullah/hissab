info:
  name: Get home
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/home"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Returns the signed-in user's Home summary and five most recent personal or shared financial rows.
