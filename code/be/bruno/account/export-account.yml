info:
  name: Export account
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/account/export"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Downloads the authenticated user's versioned JSON account snapshot.
