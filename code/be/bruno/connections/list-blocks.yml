info:
  name: List blocked users
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/v1/blocks"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Lists users blocked by the authenticated user.

