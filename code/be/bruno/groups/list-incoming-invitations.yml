info:
  name: List incoming invitations
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/v1/group-invitations"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Lists pending group invitations received by the authenticated user.
