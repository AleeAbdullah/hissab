info:
  name: List groups
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/v1/groups"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Lists groups associated with the authenticated user.
