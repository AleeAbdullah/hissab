info:
  name: List group invitations
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/v1/groups/{{groupId}}/invitations"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Lists pending invitations for a group managed by an active member.
