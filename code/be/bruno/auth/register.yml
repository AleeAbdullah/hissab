info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/v1/auth/register"
  headers:
    - name: Content-Type
      value: application/json
    - name: Idempotency-Key
      value: "{{$randomUUID}}"
  body:
    type: json
    data: |-
      {
        "email": "{{email}}",
        "password": "{{password}}",
        "displayName": "{{displayName}}",
        "timezone": "{{timezone}}",
        "deviceName": "{{deviceName}}"
      }
runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.status === 201) {
          bru.setVar("accessToken", res.body.accessToken);
          bru.setVar("refreshToken", res.body.refreshToken);
          bru.setVar("sessionId", res.body.sessionId);
        }

docs: Creates a user and captures the returned access token, refresh token, and session ID.
