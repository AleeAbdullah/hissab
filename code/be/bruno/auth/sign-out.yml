info:
  name: Sign out
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/v1/auth/sign-out"
  headers:
    - name: Idempotency-Key
      value: "{{$randomUUID}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Revokes the current login session.

