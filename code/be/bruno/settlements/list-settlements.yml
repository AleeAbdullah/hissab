info:
  name: List settlements
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/v1/ledgers/{{ledgerId}}/settlements?limit=50"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Lists latest settlement revisions using cursor pagination.
