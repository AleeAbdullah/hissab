info:
  name: Get settlement
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/settlements/{{settlementId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Reads the latest revision of one settlement.
