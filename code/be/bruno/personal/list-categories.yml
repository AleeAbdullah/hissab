info:
  name: List personal categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/personal/categories"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Lists the fixed system income and expense categories available to personal transactions.
