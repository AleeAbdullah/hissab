info:
  name: Delete personal transaction
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/v1/personal/transactions/{{personalTransactionId}}?expectedVersion=2"
  headers:
    - name: Idempotency-Key
      value: "{{$randomUUID}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Creates an auditable tombstone revision for the owned transaction.
