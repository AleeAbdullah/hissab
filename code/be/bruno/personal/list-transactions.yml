info:
  name: List personal transactions
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/personal/transactions?limit=50"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Lists latest personal transaction revisions with optional date, type, category, and cursor filters.
