info:
  name: Get personal transaction
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/v1/personal/transactions/{{personalTransactionId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Gets the latest immutable revision for one owned personal transaction.
