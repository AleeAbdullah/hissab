info:
  name: List ledger balances
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/v1/ledgers/{{ledgerId}}/balances"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Computes every historically joined member's net balance per currency for one ledger.
