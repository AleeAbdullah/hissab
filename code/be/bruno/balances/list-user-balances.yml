info:
  name: List my balances
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/balances"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Computes the authenticated user's net balance per ledger and currency from immutable postings.
