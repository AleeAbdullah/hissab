info:
  name: Get shared expense
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/v1/expenses/{{expenseId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Reads the latest revision of one shared expense.
