info:
  name: List shared expenses
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/ledgers/{{ledgerId}}/expenses?limit=50"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Lists latest expense revisions using cursor pagination.
