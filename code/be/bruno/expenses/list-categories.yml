info:
  name: List shared-expense categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/shared-expense-categories"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Lists the fixed system categories accepted by shared expenses.
