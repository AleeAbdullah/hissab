info:
  name: List activity
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/activity?limit=50"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Lists visible shared financial, group, and connection events with opaque cursor pagination. Optional filters are area and ledgerId.
