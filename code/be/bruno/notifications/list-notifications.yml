info:
  name: List notifications
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/notifications?limit=50"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Lists the current user's durable in-app notifications with opaque cursor pagination.
