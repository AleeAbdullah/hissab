info:
  name: Mark all notifications read
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/v1/notifications/read-all"
  headers:
    - name: Idempotency-Key
      value: "{{$randomUUID}}"
  auth:
    type: bearer
    token: "{{accessToken}}"
