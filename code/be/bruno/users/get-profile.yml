info:
  name: Get profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/users/me"
  auth:
    type: bearer
    token: "{{accessToken}}"

docs: Returns the authenticated user's profile and preferences.

