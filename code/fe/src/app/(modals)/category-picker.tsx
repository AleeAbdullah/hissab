import { ComingLaterScreen } from '@/features/coming-later/screen';
export default function CategoryPickerScreen() {
  return (
    <ComingLaterScreen
      eyebrow="EXPENSE SETUP"
      title="Open a ledger to choose a category"
      purpose="System categories are available while adding an expense from a friend or group ledger."
    />
  );
}
