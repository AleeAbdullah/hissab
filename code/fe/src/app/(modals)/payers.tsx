import { ComingLaterScreen } from '@/features/coming-later/screen';
export default function PayersScreen() {
  return (
    <ComingLaterScreen
      eyebrow="EXPENSE SETUP"
      title="Open a ledger to add payers"
      purpose="Payer amounts are set while adding an expense from a friend or group ledger."
    />
  );
}
