import { ComingLaterScreen } from '@/features/coming-later/screen';
export default function ReportModeScreen() { return <ComingLaterScreen purpose="Personal report mode will be applied when report calculations are implemented. The account default is already editable in Profile." links={[{ label: 'Edit account default', href: '/account/profile' }]} />; }
