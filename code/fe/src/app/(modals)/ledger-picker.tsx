import { ComingLaterScreen } from '@/features/coming-later/screen';
export default function LedgerPickerScreen() {
  return (
    <ComingLaterScreen
      eyebrow="EXPENSE SETUP"
      title="Choose a ledger first"
      purpose="Open a friend or group, then use Add expense. Hissab creates expenses in that active ledger."
    />
  );
}
