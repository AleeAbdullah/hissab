import { ComingLaterScreen } from '@/features/coming-later/screen';
export default function SplitScreen() { return <ComingLaterScreen purpose="Equal, exact and percentage splits need live ledger members and backend rounding rules." />; }
