import { ComingLaterScreen } from '@/features/coming-later/screen';

export default function GroupsScreen() {
  return <ComingLaterScreen eyebrow="GROUPS" title="The Groups app experience is coming later" purpose="A group is a shared ledger for a trip, home, or recurring plan." note="Group and membership APIs are available, but this screen is not connected to them yet." actionLabel="Create group · coming later" />;
}
