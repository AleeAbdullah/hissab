import { ComingLaterScreen } from '@/features/coming-later/screen';
export default function GroupSettingsScreen() { return <ComingLaterScreen eyebrow="GROUP SETTINGS" title="Group settings in the app are coming later" purpose="Balance-aware leaving and archival are available through the API, but this screen is not connected yet." />; }
