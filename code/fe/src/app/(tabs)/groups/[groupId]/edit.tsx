import { ComingLaterScreen } from '@/features/coming-later/screen';
export default function EditGroupScreen() { return <ComingLaterScreen eyebrow="GROUPS" title="Editing groups in the app is coming later" purpose="Group name updates are available through the API, but this screen is not connected yet." />; }
