import { ComingLaterScreen } from '@/features/coming-later/screen';
export default function SimplifiedDebtsScreen() {
  return (
    <ComingLaterScreen
      eyebrow="SIMPLIFIED DEBTS"
      title="Debt simplification is coming later"
      purpose="Hissab now shows authoritative balances, but the backend does not yet expose simplified transfer suggestions."
    />
  );
}
