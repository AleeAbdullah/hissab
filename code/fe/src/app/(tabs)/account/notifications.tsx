import { ComingLaterScreen } from '@/features/coming-later/screen';

export default function NotificationsScreen() {
  return <ComingLaterScreen purpose="Push permissions and notification preferences need backend delivery support before this screen can save anything." />;
}
